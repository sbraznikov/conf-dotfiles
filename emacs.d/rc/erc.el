(require 'ssl)

;; (defun irka ()
;;   (interactive)
;;   (setq erc-auto-query 'buffer)
;;   (setq erc-track-exclude-types '("NICK"))
;;   (setq erc-track-use-faces t)
;;   (setq erc-track-faces-priority-list '(erc-current-nick-face))
;;   (setq erc-autojoin-channels-alist '(("#test")))
;;   (erc-ssl :server irc-work-server :port 9999
;;            :nick irc-work-nick :password irc-work-pass
;;            :full-name irc-work-full-name))
