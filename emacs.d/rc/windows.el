(defun my-max-frame-latop ()
  (interactive)
  (set-frame-position (selected-frame) 220 0)
  (set-frame-size (selected-frame) 120 51))

(defun my-max-frame-display ()
  (interactive)
  (set-frame-position (selected-frame) 230 70)
  (set-frame-size (selected-frame) 140 50))

